[project]
name = "context-pipe-redis"
version = "0.1.0"
description = "Add your description here"
authors = [
    { name = "sarvesh4396", email = "heysarvesh@pm.me" }
]
requires-python = ">=3.14"
dependencies = [
    "redis[asyncio]>=7.4.0",
]

[build-system]
requires = ["uv_build>=0.11.3,<0.12.0"]
build-backend = "uv_build"
