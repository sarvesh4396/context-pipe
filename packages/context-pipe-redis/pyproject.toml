[project]
name = "context-pipe-redis"
version = "0.1.0"
description = "Redis backend for context-pipe"
authors = [
    { name = "Sarvesh Kr. Dwivedi", email = "heysarvesh@pm.me" }
]
requires-python = ">=3.11"
dependencies = [
    "context-pipe==0.1.0",
    "redis[asyncio]>=7.4.0",
]

[build-system]
requires = ["uv_build>=0.11.3,<0.12.0"]
build-backend = "uv_build"
