[project]
name = "context-pipe-memory"
version = "0.1.0"
description = "In-memory backend for context-pipe"
authors = [
    { name = "Sarvesh Kr. Dwivedi", email = "heysarvesh@pm.me" }
]
requires-python = ">=3.11"
dependencies = [
    "context-pipe==0.1.0",
]

[build-system]
requires = ["uv_build>=0.11.3,<0.12.0"]
build-backend = "uv_build"
